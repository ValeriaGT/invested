{
  "buildCommand": "cd invested-app && npm install && npm run build",
  "outputDirectory": "invested-app/dist",
  "installCommand": "echo skip-root-install",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
